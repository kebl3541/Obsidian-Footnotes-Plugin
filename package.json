{
  "name": "footnote-inline-editor",
  "version": "1.2.1",
  "description": "Insert and edit Obsidian footnotes inline via a popup.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "esbuild src/footnotes.ts --format=esm --outfile=test/footnotes.build.mjs && node test/footnotes.test.mjs"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "footnote"
  ],
  "author": "kebl3541",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.20.0",
    "obsidian": "^1.4.11",
    "typescript": "^5.3.3"
  }
}
